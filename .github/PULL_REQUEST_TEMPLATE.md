## What changes

<!-- The case that now parses, or the date that was wrong. -->

## Checklist

- [ ] Every case comes with a test, negative cases included
- [ ] `swift test` passes
- [ ] `swift format lint --recursive --strict Sources Tests` passes
- [ ] The change is a new rule or a new table row, not an exception inside another rule
